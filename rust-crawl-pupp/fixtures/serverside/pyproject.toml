[tool.poetry]
name = "fixtures"
version = "0.1.0"
description = ""
authors = ["Artur Das"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
black = "^24.4.2"
flask = "^3.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
